#!/bin/sh
# Prevent committing anything under .obsidian/
if git diff --cached --name-only | grep -qE '^\.obsidian/'; then
  echo "Error: .obsidian/ should not be committed. Unstage those files." >&2
  exit 1
fi
exit 0
